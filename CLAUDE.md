# CLAUDE.md — ฟอร์มสำรวจรายชื่อแขก งานแต่ง Ka-nan & Poom

## นี่ไม่ใช่การ์ดแต่งงาน

เป็นหน้าเว็บสำรวจก่อนพิมพ์การ์ดจริง ส่งลิงก์ให้แขกทางไลน์
เป้าหมายเดียว: **รู้ว่าใครมา มากี่คน จะส่งการ์ดจริงไปที่ไหน และพิมพ์ชื่อว่าอะไร**

การ์ดฉบับจริงเป็นกระดาษ ทำทีหลัง คนละงานกัน
หน้านี้ **ไม่ต้องมี** กำหนดการ/ตารางเวลางาน แกลเลอรี นับถอยหลัง QR ให้ของขวัญ — ถ้าเผลอใส่ ให้ตัดออก
**ข้อยกเว้นที่เจ้าของขอเพิ่มเอง (ของจริง ไม่ใช่เดา):** ลิงก์ Google Maps ไปสถานที่จัดงาน + dress code แบบสวอตช์สี — อยู่บนหน้าปก ใต้ปุ่ม RSVP ไม่ใช่กำหนดการเต็มรูปแบบ

- คู่บ่าวสาว: **Ka-nan & Poom** (ไทย: คะแนน & ภูมิ) — ลำดับชื่อบนเว็บคือ Ka-nan ก่อน Poom (เจ้าของขอเปลี่ยนจากเดิมที่เป็น Poom & Kanan)
- วันงาน: **วันอาทิตย์ที่ 27 ธันวาคม 2569 / 27 December 2026**
- แขกประมาณ 200 คน
- สถานที่: **Ava Trivi Studio** (มีลิงก์ Google Maps ในหน้าปกแล้ว) — ส่วนกำหนดการ/เวลางาน **ยังไม่สรุป** อย่าใส่ของปลอมลงไป

---

## สถานะปัจจุบันของโค้ด

ตรวจไฟล์จริงในโฟลเดอร์ก่อนเริ่มงานทุกครั้ง อย่าสมมติว่าทำไปถึงไหนแล้วจากเอกสารนี้อย่างเดียว ตอนนี้ (ล่าสุด):

- **ใช้งานได้จริงแล้วแบบ end-to-end และผ่าน Definition of Done เกือบทุกข้อแล้ว** — ทดสอบส่งฟอร์มจริงหลายเคส, เวลาไทยถูกต้อง, เปิดจาก LINE in-app browser จริงผ่านแล้ว, วัดน้ำหนักหน้าเว็บจาก Network tab จริงแล้ว (เจอและแก้บั๊ก `image-set()` ทำมือถือโหลดไฟล์หนักผิดจากที่ตั้งใจ — ดูหัวข้อรูปภาพ), เช็ค console แล้วไม่มี error จากโค้ดเรา (มี warning จาก wrapper ของ Google เองซึ่งแก้ไม่ได้และไม่ต้องแก้) — **ยังไม่เคยทดสอบส่งซ้ำสองครั้งติดกันว่าไม่พัง** อย่าข้ามข้อนี้ใน Definition of done
- เป็น git repo แล้ว push ขึ้น GitHub repo สาธารณะ `ppuvitt/poom-kanan-rsvp` แล้ว (`main` branch) — ทุกครั้งที่แก้ต้อง `git push` แล้ว `clasp push` แล้ว `clasp create-deployment -i <deploymentId>` ต่อ (ดู deployment ID ปัจจุบันจาก `clasp list-deployments` ห้ามสร้าง deployment ใหม่ซ้ำ ให้ redeploy ตัวเดิม)
- `gh` CLI ติดตั้งและ login ไว้แล้ว (`gh auth setup-git` ผูกกับ git แล้ว) ใช้ push ได้เลยไม่ต้อง login ใหม่
- Script Property `SHEET_ID` ตั้งค่าไว้แล้วใน Apps Script editor (ไม่ได้เก็บค่าไว้ในไฟล์ใดๆ ตามกฎ — ถ้าต้องรู้ค่าจริงต้องถามเจ้าของหรือเข้าไปดูเองในหน้า Script Properties)
- รูปที่ใช้จริงตอนนี้: `cover`, `rsvp`, `thankyou` แต่ละจุดมี 2 ใบสลับกัน (ดูหัวข้อ "สไลด์โชว์รูป") ครบทั้ง 1x/2x/jpg สำรอง อยู่ใน `assets-src/` และขึ้น jsDelivr แล้วจริง
- มีเพลงพื้นหลังจริงแล้ว (`assets-src/bgm.mp3` สร้างจาก Gemini/Lyria ยืนยันว่าไม่มีเสียงร้อง) ปุ่ม `#sound` เล่น/หยุดได้จริง เริ่มปิดเสียง มีป้าย `.sound-hint` เตือนก่อนกดครั้งแรกแล้วจางหายไปเอง
- หน้าปกมีลิงก์ Google Maps (Ava Trivi Studio) และ dress code แบบสวอตช์สีแล้ว (ดูหัวข้อด้านบน)
- ปุ่ม "← ย้อนกลับ" อยู่ใน `<form id="rsvp">` (เป็น `type="button"` กันไม่ให้ submit ฟอร์มโดยไม่ตั้งใจ) หายไปอัตโนมัติพร้อมฟอร์มตอนส่งสำเร็จ — หน้า Thank you มีปุ่ม "กลับหน้าแรก →" ของตัวเองแยกต่างหาก (มุมขวาล่าง, `#restart`) ที่ไม่ได้แค่พาไปหน้าปก แต่ **รีเซ็ตฟอร์มทั้งหมดด้วย** (`form.reset()` + `renderCompanionInputs()` ใหม่ + สลับรูปกลับเป็น `photo--rsvp`) ออกแบบมาเผื่อกรณีคนกรอกแทนคนอื่นหลายคนต่อกันบนเครื่องเดียว
- **ฟิลด์เกี่ยวกับการจัดส่งการ์ด/ที่อยู่/เบอร์โทรถูกตัดออกทั้งหมดแล้ว** (เจ้าของตัดสินใจไม่เก็บข้อมูลนี้ผ่านฟอร์มอีกต่อไป) แทนที่ด้วยช่องชื่อผู้ติดตาม (ไดนามิกตามจำนวน) กับช่องแพ้อาหาร — ดูตารางฟิลด์ด้านล่าง

---

## เริ่มจากไฟล์ที่มีอยู่ อย่าเขียนใหม่จากศูนย์

`Index.html` ในโฟลเดอร์นี้คือดีไซน์ที่เจ้าของอนุมัติแล้ว — โครงสร้าง สี ฟอนต์ ฟอร์ม การแบ่งครึ่งจอ ใช้งานได้ครบ
ให้ต่อยอดจากไฟล์นี้ ห้ามรื้อเขียนใหม่เพราะคิดว่ามีวิธีที่ดีกว่า
สิ่งที่ยังขาดคือการต่อกับ Google Sheet รูปจริง เพลง และลายมุม

---

## สถาปัตยกรรม

Google Apps Script web app ตัวเดียว ไม่มีอย่างอื่น

```
มือถือแขก → doGet เสิร์ฟ Index.html → google.script.run → Google Sheet
```

**ห้ามทำ**
- ห้ามเสนอ static hosting, Cloudflare Pages, Netlify, doPost, CORS workaround — พิจารณาแล้วและตัดทิ้ง
- ห้ามใส่ framework, bundler, build step
- ห้ามเพิ่ม analytics, cookie banner
- ห้ามใช้ localStorage / sessionStorage

**รูปและเพลงเก็บใน Apps Script ไม่ได้** มันเสิร์ฟได้แค่ HTML/JS/CSS
ไฟล์ binary ไปอยู่บน GitHub repo สาธารณะ แล้วเรียกผ่าน jsDelivr
ห้ามใช้ Google Drive เป็นที่เก็บรูป ลิงก์มันเปลี่ยนเองและโดน throttle

```
rsvp/
├─ CLAUDE.md
├─ appsscript.json
├─ Code.gs
├─ Index.html
└─ assets-src/     ← ไฟล์ต้นฉบับ ใส่ใน .claspignore ไม่ต้อง push
```

---

## โครงหน้าเว็บ

สองหน้าจอในไฟล์เดียว สลับด้วย JS ไม่มีการโหลดหน้าใหม่

**หน้า 1 — ปก** รูปคู่บ่าวสาว + ชื่อ + วันที่ + ประโยคอธิบาย + ปุ่ม RSVP
**หน้า 2 — ฟอร์ม** โครงเดียวกับหน้าปก แต่เปลี่ยนรูป

ทั้งสองหน้าใช้คลาส `.split` ร่วมกัน:
- **มือถือ** ซ้อนบนล่าง — หน้าปกและหน้าฟอร์มมีรูปด้านบน 56svh เหมือนกันทั้งคู่ (**เปลี่ยนใจแล้ว** เดิมหน้าฟอร์มตั้งใจไม่มีรูปเพื่อให้เห็นช่องกรอกทันที แต่เจ้าของขอให้ใส่กลับมาเพื่อความสวยงาม พร้อมไอคอนลูกศรกระพริบเตือนให้เลื่อนลง `.scroll-hint` ที่มุมล่างของรูปหน้าฟอร์ม — element ตัวเดียวกับรูป ใช้ `.photo--thankyou .scroll-hint{display:none}` ซ่อนตอนเป็นหน้า Thank you เพราะไม่มีอะไรให้เลื่อนกรอกต่อแล้ว)
- **จอ ≥900px** แบ่งซ้ายขวา รูป 52% ซ้าย (ค้างอยู่กับที่ตอนเลื่อน) เนื้อหา 48% ขวา

ระวัง: `#rsvp-view.on` ต้องเป็น `display:flex` ไม่ใช่ `block` ไม่งั้นการแบ่งซ้ายขวาจะไม่ทำงาน (เคยพลาดมาแล้ว)

---

## ภาษา

- **หน้าปกภาษาอังกฤษล้วน** ชื่อ วันที่ ประโยคเชิญ ปุ่ม
- **หน้าฟอร์มภาษาไทยล้วน** ยกเว้นหัวข้อ `RSVP` ที่คงไว้เป็นอังกฤษเพื่อความต่อเนื่อง
- ข้อความ error ทั้งหมดเป็นภาษาไทย

---

## ดีไซน์ที่อนุมัติแล้ว

โทนพาสเทลชมพู–ทอง–ฟ้าจาง อ้างอิงจากการ์ดแต่งงานสไตล์เจ้าหญิงที่เจ้าของเลือกไว้

| อย่าง | ค่า |
|---|---|
| พื้นครีม | `#FCF6F1` |
| ขอบชมพู | `#EDCECB` |
| ชมพูเข้ม (เตือน/บังคับกรอก) | `#C98C8C` |
| ฟ้าเทา | `#7E9CBC` |
| ตัวอักษรหลัก | `#4E3E3C` |
| ตัวอักษรรอง | `#8B7573` |
| ทอง | `#C09B52` / เข้ม `#9A7734` |

ไล่เฉดพื้นหลังแผงข้อความ: ฟ้าจาง `#E9F0F7` → ชมพูจาง `#FAEFEC` → ครีม

| ฟอนต์ | ใช้กับ |
|---|---|
| Great Vibes | ชื่อคู่บ่าวสาวบนปก และคำว่า Thank you |
| Cinzel | วันที่ ปุ่ม RSVP หัวข้อฟอร์ม |
| Cormorant Garamond | เนื้อความภาษาอังกฤษ |
| IBM Plex Sans Thai | ทุกอย่างในหน้าฟอร์ม |

**Motion** — อัปเดตแล้ว (เจ้าของขอเพิ่มลูกเล่นทีหลัง เดิมกฎนี้เคยจำกัดไว้จุดเดียว): ตอนนี้มี fade+rise-in (CSS `@keyframes riseIn`, opacity/transform เท่านั้น ไม่ใช้ transition ข้าม `display:none`) เล่นตอน (1) โหลดหน้าปกครั้งแรก (2) กด RSVP เข้าหน้าฟอร์ม/กด back กลับหน้าปก (3) ส่งฟอร์มสำเร็จเข้าหน้า Thank you — ทั้งหมดยังต้องเคารพ `prefers-reduced-motion` เหมือนเดิม (มี media query ปิด animation ทั้งหมดอยู่ท้ายไฟล์ CSS แล้ว)
ห้ามใส่ fade-in ตอน scroll (ข้อนี้ยังคงอยู่ — ลูกเล่นทั้งหมดผูกกับการเปลี่ยนหน้า/โหลดหน้าเท่านั้น ไม่ผูกกับ scroll position)

---

## ลายกุหลาบมุม — เคยพังมาแล้ว อ่านก่อนทำ

เจ้าของอยากได้ลายกุหลาบกับเถาไอวี่สีทองที่มุมบนซ้ายและล่างขวา ตอนนี้ **ถอดออกชั่วคราว** เพราะทำไม่สำเร็จสองรอบ

**สาเหตุที่พัง**
1. วาดด้วย SVG แล้วออกมาแข็ง ไม่ได้ความละมุนของภาพสีน้ำ — **ห้ามวาดลายดอกไม้ด้วย SVG อีก**
2. เอารูปมาลบพื้นหลังเองด้วยการเทียบระยะห่างของสี ลบไม่หมด เหลือขอบสี่เหลี่ยมทึบเห็นชัด
3. วางใหญ่เกินไปจนทับตัวหนังสือ ตัวอักษรตัวแรกของหัวข้อหายไปครึ่งตัว

**กฎเวลาทำใหม่**
- ใช้ไฟล์ **PNG พื้นหลังโปร่งตั้งแต่ต้นทาง** เท่านั้น (Freepik / Vecteezy ค้น "watercolor rose gold corner border png transparent") ห้ามลบพื้นหลังเอง
- ต้องเป็นลายมุมสำเร็จรูป ไม่ใช่การตัดชิ้นส่วนจากกรอบเต็มใบ
- ความกว้างบนมือถือไม่เกิน 110px บนเดสก์ท็อปไม่เกิน 180px
- **ห้ามทับพื้นที่ตัวหนังสือ** ทดสอบที่ 390px แล้วตัวอักษรทุกตัวต้องอ่านครบ
- `opacity` ไม่เกิน 0.9 และวาง `z-index` ต่ำกว่าเนื้อหาเสมอ
- แปลงเป็น WebP ไฟล์ละไม่เกิน 60KB

---

## รูปภาพและเพลง

### รูป
- ใช้ 3 จุด (`cover`, `rsvp`, `thankyou`) — รูปหน้าฟอร์มและหน้า Thank you แสดงบนทุกขนาดจอแล้ว (มือถือก็มีรูปด้วย ไม่ใช่แค่เดสก์ท็อปเหมือนเดิม) **แต่ละจุดมีรูป 2 ใบสลับกันแบบ crossfade ทุก 5 วินาที** (ตั้งชื่อไฟล์ `<ชื่อ>` กับ `<ชื่อ>-b` เช่น `cover.webp`/`cover-b.webp`) รวมเป็น 6 คู่ภาพ (1x+2x) ต่อโปรเจกต์ ไม่ใช่ 3 — ดูหัวข้อ "สไลด์โชว์รูป" ด้านล่างก่อนเพิ่ม/เปลี่ยนรูปอีก
- ย่อกว้างสุด 1200px แปลง WebP **ไฟล์ละไม่เกิน 250KB** เตรียม JPG สำรอง
- ใส่ `width`/`height` ในแท็กเสมอ กัน layout กระตุก
- **น้ำหนักหน้าเว็บรวมไม่เกิน 1.2MB** ไม่นับเพลง ถ้าเกินให้ตัดรูป

### สูตรย่อรูป — ทำแล้วได้ผล เจอปัญหาเบลอ+รูปแนวตั้งมาแล้ว อ่านก่อนเปลี่ยนรูปใหม่

ต้นฉบับสูงมาก (~5000px+) เก็บไว้ใน `pic/` (gitignore ไว้ ไม่ commit) ผ่าน `assets-src/` เป็นไฟล์ที่ประมวลผลแล้วเท่านั้น

**ต้นฉบับไม่ได้เป็นแนวนอนเสมอไป** (เจอรูปแนวตั้งมาแล้วรอบหนึ่ง) — ห้ามใช้ `sips -Z` (ย่อตามด้านที่ยาวที่สุด) เพราะรูปแนวตั้งจะโดนย่อตามความสูงแทนความกว้าง ทำให้ไฟล์กว้างแคบผิดปกติ **ให้ใช้ `sips --resampleWidth` เสมอ** เพื่อยึดความกว้างเป็นหลักไม่ว่ารูปจะแนวไหน:

```bash
# 1) มือถือ + เดสก์ท็อปจอทั่วไป (1x) — กว้าง 1200px ตามเพดาน 250KB
sips --resampleWidth 1200 pic/pic_01.JPG --out assets-src/cover-src.jpg
cwebp -q 78 assets-src/cover-src.jpg -o assets-src/cover.webp
sips -s format jpeg -s formatOptions 78 assets-src/cover-src.jpg --out assets-src/cover.jpg   # JPG สำรอง

# 2) เดสก์ท็อป (2x) — กว้าง 2200px ใช้แก้เบลอบนจอเดสก์ท็อป
sips --resampleWidth 2200 pic/pic_01.JPG --out assets-src/cover-2x-src.jpg
cwebp -q 72 assets-src/cover-2x-src.jpg -o assets-src/cover@2x.webp
```
ทำซ้ำแบบเดียวกันกับรูปอื่น → `rsvp.webp`/`rsvp.jpg`/`rsvp@2x.webp` และ `thankyou.webp`/`thankyou.jpg`/`thankyou@2x.webp` (เปลี่ยนแค่ชื่อไฟล์นำเข้า/ส่งออก)

**ทำไมต้องมี 2 ขนาด — และทำไมห้ามใช้ `image-set()` เลยแม้แต่นิดเดียว (เจอบั๊กจริงมาแล้ว กระทบมือถือทุกเครื่อง ไม่ใช่แค่จอใหญ่)**

รอบแรก: เคยลองใช้ `image-set()` ให้เบราว์เซอร์เลือกไฟล์เองตาม pixel density (`1x`/`2x`) แล้วจอ PC จอใหญ่ที่ไม่ใช่ Retina (DPR=1) ยังเบลออยู่ดี เพราะ `image-set()` เลือกไฟล์จาก **ความหนาแน่นพิกเซลของจอ อย่างเดียว ไม่สนความกว้างของกล่องที่แสดงจริง** ตอนนั้นแก้โดยเพิ่ม override ใน `@media (min-width:900px)` ให้เดสก์ท็อปใช้ไฟล์ `@2x` ตรงๆ แต่ **ยังเก็บ `image-set()` ไว้ในกฎเริ่มต้น (นอก media query) เผื่อจอเล็กที่เป็น Retina** — ตรงนี้คือจุดพัง

รอบสอง (เจอจริงตอนวัดน้ำหนักหน้าเว็บจาก Network tab): เปิด DevTools ที่ viewport 400px (จำลองมือถือ) แต่เครื่องทดสอบเป็นจอ Retina (DPR=2) ผลคือ **โหลดไฟล์ `@2x` (~365KB) แทนที่จะเป็นไฟล์มือถือ (~155KB)** เพราะ `image-set()` ในกฎเริ่มต้นยังทำงานอยู่ ยึด DPR ของจออย่างเดียวเหมือนเดิม ไม่สนว่า viewport กว้างแค่ 400px — **มือถือจริงแทบทุกรุ่นเป็นจอ DPR 2–3 อยู่แล้ว แปลว่าตลอดมาที่ผ่านมา มือถือไม่เคยได้โหลดไฟล์เบาตามที่ตั้งใจไว้เลยสักครั้ง** งบ 1.2MB ที่คำนวณไว้ก่อนหน้านี้ผิดจากความเป็นจริงมาก

**ทางแก้ที่ใช้อยู่ตอนนี้:** ตัด `-webkit-image-set()`/`image-set()` ออกจากกฎเริ่มต้นทั้งหมด เหลือแค่ `background-image:url(...)` ธรรมดาชี้ไปไฟล์มือถือ (1200px) ตรงๆ ปล่อยให้ `@media (min-width:900px)` เป็นตัวตัดสินอย่างเดียวว่าจะใช้ไฟล์ไหน (ยึด**ความกว้าง viewport เท่านั้น ไม่ใช่ DPR**) — 1200px กว้างพอสำหรับจอมือถือ DPR สูงอยู่แล้ว (390 CSS px × 3 ≈ 1170px ใกล้เคียง 1200px มาก) ไม่ต้องพึ่ง `image-set()` เลย

**กฎเหล็ก: ห้ามใส่ `-webkit-image-set()`/`image-set()` กลับเข้าไปในกฎเริ่มต้น (นอก `@media (min-width:900px)`) อีกเด็ดขาด** ต่อให้ดูเหมือนจะช่วยจอ retina เล็กๆ ก็ตาม เพราะมันจะไปโดนมือถือจริงทุกเครื่องด้วย — ไฟล์ desktop tier (~350KB/~150KB ต่อรูป) **เกินเพดาน 250KB ที่ตั้งไว้ข้างบนโดยตั้งใจ** แต่กระทบเฉพาะจอ ≥900px เท่านั้นแล้วตอนนี้ (มือถือปลอดภัยจริงแล้ว) — ถ้าจะเปลี่ยนรูปใหม่ ทำสูตรนี้ซ้ำแล้วอัปโหลดทั้ง 3 ไฟล์ (`.webp` มือถือ, `.jpg` สำรอง, `@2x.webp` เดสก์ท็อป) ต่อรูป แล้วเช็คด้วยว่ากฎ CSS ทั้งสองจุด (เริ่มต้น + `@media`) ใช้แค่ `url()` เปล่าๆ ไม่มี `image-set` โผล่มาอีก

โฮสต์ผ่าน jsDelivr จาก GitHub repo `ppuvitt/poom-kanan-rsvp` (public) พาธ `assets-src/<ชื่อไฟล์>` — URL รูปแบบ `https://cdn.jsdelivr.net/gh/ppuvitt/poom-kanan-rsvp@main/assets-src/<ชื่อไฟล์>` (ต้อง `git push` ก่อน jsDelivr ถึงจะเห็นไฟล์ใหม่)

**ถ้าเปลี่ยนรูปแต่ใช้ชื่อไฟล์เดิม (เช่นเปลี่ยนรูป `rsvp.webp` เป็นรูปใหม่) เจอปัญหาแคช 2 ชั้นซ้อนกัน — เจอเคสจริงมาแล้ว รูปไม่เปลี่ยนทั้งที่ทำถูกทุกขั้นตอน:**
1. **แคชฝั่ง jsDelivr (CDN)** — ต้อง purge หลัง push ทุกครั้งที่ใช้ชื่อไฟล์ซ้ำ:
   ```bash
   curl "https://purge.jsdelivr.net/gh/ppuvitt/poom-kanan-rsvp@main/assets-src/<ชื่อไฟล์ที่เปลี่ยน>"
   ```
   แล้วเช็คด้วย `curl -sI` ว่า `content-length` ตรงกับไฟล์ใหม่จริง
2. **แคชฝั่ง browser ของคนเปิดเว็บ** — purge ฝั่ง jsDelivr **ไม่ช่วยอะไรเลย** เพราะ jsDelivr ส่ง `cache-control: max-age=604800` (7 วัน) ให้ browser เก็บเองไว้แล้ว ใครเปิดลิงก์นี้มาก่อนหน้าจะยังเห็นรูปเก่าจน cache หมดอายุ ทางแก้ถาวรคือ **เติม query string ต่อท้าย URL แล้วเพิ่มเลขเวอร์ชันทุกครั้งที่เปลี่ยนไฟล์ชื่อเดิม** เช่น `rsvp.webp?v=2` → ครั้งหน้าเปลี่ยนอีกก็ขยับเป็น `?v=3` (ต้องแก้ทั้ง 3 จุดที่อ้างถึงไฟล์นั้น: base rule, `image-set()`, และ override ใน `@media (min-width:900px)`) วิธีนี้บังคับให้ทั้ง CDN และ browser มองเป็นคนละ URL ทันที ไม่ต้องพึ่งให้คนกด hard refresh เอง
   - ไฟล์ที่ตั้งชื่อใหม่ไม่เคยใช้มาก่อน (เช่น `thankyou.webp`) ไม่มีปัญหานี้ ไม่ต้อง purge ไม่ต้องมี `?v=`

### หน้า Thank you มีรูปพื้นหลังของตัวเอง (`.photo--thankyou`)
สลับจาก `.photo--rsvp` เป็น `.photo--thankyou` ด้วย JS ตอน `submitRsvp` สำเร็จ (`classList.replace('photo--rsvp','photo--thankyou')`) — เพื่อให้ animation `slideInRight` เล่นซ้ำตอนสลับรูป **animation ต้องผูกกับ selector เฉพาะคลาสรูป** (`#rsvp-view.on .photo--rsvp` / `#rsvp-view.on .photo--thankyou` แยกกัน) ห้ามผูกกับ `.split__photo` เฉยๆ เพราะแบบนั้น animation จะไม่ retrigger ตอนสลับคลาส (คลาสยังแมตช์ selector เดิมต่อเนื่อง ไม่มีการเปลี่ยนค่า computed animation-name)

### สไลด์โชว์รูป — แต่ละจุดมี 2 ภาพสลับกันทุก 5 วิ
โครงสร้าง: ในแต่ละ `.split__photo` มี `<div class="slide slide--a active">`/`<div class="slide slide--b">` เป็นลูกตัวแรกๆ (ก่อน `.overlay`) แต่ละ `.slide` เป็น `position:absolute;inset:0` ตั้ง `background-image` แยกกันผ่าน selector `.photo--cover .slide--a` / `.photo--cover .slide--b` เป็นต้น (ไม่ได้ตั้ง background-image ที่ตัว `.photo--cover` เองแล้วเหมือนก่อนหน้านี้) สลับด้วย JS ทุก 5000ms โดยเพิ่ม/ลบคลาส `.active` (มี `transition:opacity` ทำให้เฟดเข้าออก)

- **`.split__photo::after` (gradient overlay) ต้องมี `z-index:1` เสมอ** ไม่งั้น slide ใหม่จะวาดทับ gradient (เจอมาแล้วตอนทำรอบแรก)
- JS เช็ค `prefers-reduced-motion` ก่อนเริ่ม `setInterval` เลย — ถ้า reduce ไว้จะไม่มีสไลด์โชว์เกิดขึ้นเลย (ไม่ใช่แค่ตัด transition เฉยๆ)
- `#rsvp-view .split__photo` ใช้ร่วมกันทั้งหน้าฟอร์มและหน้า Thank you ตัว interval เดียวรันตลอด ไม่ต้อง restart ตอนสลับ `.photo--rsvp`↔`.photo--thankyou` — พอสลับคลาสฝั่งพ่อ `background-image` ของ slide ที่กำลัง active จะเปลี่ยนตามทันที (ไม่มี fade ตอนสลับพอดี ยอมรับได้)
- เพิ่ม/เปลี่ยนรูปคู่ไหนต้องทำสูตรย่อรูปให้ครบทั้ง `<ชื่อ>` และ `<ชื่อ>-b` (1x + 2x ทั้งคู่ = 4 ไฟล์ต่อจุด) แล้วเพิ่ม CSS rule คู่ `.photo--X .slide--a`/`.photo--X .slide--b` ทั้งใน base และใน `@media (min-width:900px)`

### เพลง
- **autoplay ที่มีเสียงถูกเบราว์เซอร์มือถือบล็อกทั้งหมด ห้ามพยายามหลบ** ถ้าเขียนโค้ดที่พยายาม autoplay แล้วอ้างว่าใช้ได้ ถือว่าผิด
- ปุ่มลำโพงมุมขวาบน (มีอยู่แล้วใน `Index.html` แต่ยังไม่มีไฟล์เสียง) **เริ่มที่สถานะปิดเสียง**
- MP3 ≤128kbps ยาว 2–3 นาที `loop` และ `preload="none"`
- เริ่มที่ `volume = 0.4`
- โหลดเพลงไม่สำเร็จให้ซ่อนปุ่มเงียบ ๆ ห้ามขึ้น error
- ใช้เพลงบรรเลง royalty-free เท่านั้น ห้ามใช้เพลงลิขสิทธิ์

---

## ฟิลด์ในฟอร์ม — หัวใจของโปรเจกต์

ห้ามตัดหรือเพิ่มข้อไหนโดยไม่ถาม ถามซ้ำรอบสองคือไล่ตาม 200 คน

| ฟิลด์ | ชนิด | บังคับ | หมายเหตุ |
|---|---|---|---|
| ชื่อ-นามสกุลของท่าน | text | ✓ | แสดงเสมอ ไม่ว่าจะมาหรือไม่มา — **เดิมชื่อฟิลด์คือ "ชื่อของท่าน" เปลี่ยนตามที่เจ้าของขอ และตอนนี้ทำหน้าที่เป็นชื่อบนการ์ดไปในตัว** (ดูหมายเหตุถัดไป) |
| ชื่อเล่น | text | — | เพิ่มทีหลังตามที่เจ้าของขอ ไม่บังคับ |
| เป็นแขกฝั่งเจ้าบ่าวหรือเจ้าสาว | dropdown: ฝั่งเจ้าบ่าว / ฝั่งเจ้าสาว | ✓ | แสดงเสมอ ไม่ว่าจะมาหรือไม่มา — ตัวเลือกแรกเป็น placeholder ที่เลือกไม่ได้ (`disabled`) บังคับให้เลือกจริง |
| ท่านจะมาร่วมงานหรือไม่ | สะดวกร่วมงาน / ไม่สะดวกร่วมงาน | ✓ | **ตัด "ยังไม่แน่ใจ" ออกแล้วตามที่เจ้าของขอ** (เคยมีไว้กันคนไม่ตอบ แต่ภายหลังตัดสินใจตัดทิ้ง) |
| มีผู้ติดตามมาด้วยกันกี่ท่าน (นับรวมท่าน) | number 1–10 | ✓ ถ้าสะดวกร่วมงาน | ซ่อนเมื่อ "ไม่สะดวกร่วมงาน" — เปลี่ยนค่าแล้ว **สร้างช่องชื่อผู้ติดตามด้านล่างขึ้นมาสด ๆ ตามจำนวน** (ดูแถวถัดไป) |
| ชื่อผู้ติดตาม + สถานะ | text + dropdown (คู่สมรส/แฟน / เพื่อน / ญาติ) ซ้ำตามจำนวน | — (แต่ละแถว ไม่บังคับทั้งชื่อและสถานะ) | **ไดนามิก ไม่ใช่ฟิลด์นิ่งตัวเดียว** — จำนวนแถว = ค่าฟิลด์ก่อนหน้า **ลบ 1** (ไม่นับตัวท่านเอง) เช่น กรอก 3 → เห็น 2 แถว สร้าง/ลบด้วย `renderCompanionInputs()` ผูกกับ `#count` — แต่ละแถวมี **ช่องชื่อ (`.companion-name`, กว้างกว่า) + dropdown สถานะ (`.companion-relation`, แคบกว่า)** วางคู่กันในแถวเดียว (`.companion-row`, flex 1.5:1) ตัวเลือกแรกของ dropdown เป็น placeholder ว่างเลือกได้ (ไม่บังคับเลือก ต่างจาก `#side` ที่ตัวแรก `disabled`) — ส่งเป็น **array ของ object `{name, relation}`** ใน payload (`companions: [...]`) ไม่ใช่ array string เปล่าๆ — `Code.gs` เป็นคนรวมเป็นสตริงเดียว `"ชื่อ (สถานะ)"` ต่อคน (หรือแค่ `"ชื่อ"` ถ้าไม่เลือกสถานะ) **ก่อน** เข้ากระบวนการแบ่ง 5 คอลัมน์ — **ไม่ได้เพิ่มคอลัมน์ใหม่สำหรับสถานะ** ยังใช้ 5 คอลัมน์ "ชื่อผู้ติดตามคนที่ 1"–"5" เดิม แค่ค่าที่เก็บมีสถานะติดวงเล็บไปด้วย คนที่ 5 เป็นต้นไปยัดรวมกันในคอลัมน์ที่ 5 คั่นด้วย `, ` เหมือนเดิม |

**บั๊กจริงที่เจอและแก้แล้ว (สำคัญ ห้ามพลาดซ้ำ):** `#count` มี `max="10"` แต่พิมพ์เลขเกิน (เช่น 15) แล้ว `renderCompanionInputs()` เดิมแค่ clamp ค่าที่ใช้คำนวณจำนวนช่องภายในฟังก์ชันเฉยๆ **ไม่ได้เขียนค่าที่ clamp แล้วกลับเข้า `#count` เอง** ทำให้ค่าดิบในช่อง input ยังเกิน max ค้างอยู่ — พอกดปุ่มส่ง (`type="submit"`) เบราว์เซอร์เจอ **native HTML5 constraint validation ว่าฟอร์ม invalid แล้วเงียบๆ ไม่ยอมยิง submit event เข้า `onsubmit` เลย ไม่มี error โชว์ ปุ่มไม่ขยับ** ดูเหมือนปุ่มพัง ทั้งที่โค้ด validation ของเราเองไม่มีปัญหา (เรียกฟังก์ชัน `onsubmit` ตรงๆ ทดสอบแล้วทำงานถูกทุกอย่าง) — เจอจากการทดสอบจริงด้วย headless browser ไม่ใช่เดา ตอนนี้ `renderCompanionInputs()` เขียนค่า clamp แล้วกลับเข้า `countEl.value = total` ทุกครั้งแล้ว แก้เรียบร้อย **บทเรียน: ฟิลด์ไหนมี `min`/`max` แล้วมี JS clamp ค่าเองเพิ่ม ต้องเขียนค่าที่ clamp แล้วกลับเข้า DOM element จริงเสมอ ห้ามแค่ clamp ตัวแปรในหน่วยความจำไว้เฉยๆ**
| ท่านหรือผู้ติดตามมีอาหารที่ทานไม่ได้ หรือแพ้อาหารอะไรไหม | textarea | — | เพิ่มทีหลังตามที่เจ้าของขอ ไม่บังคับ อยู่ในกลุ่มสะดวกร่วมงานเท่านั้น (ซ่อนพร้อมกลุ่มนี้เมื่อไม่สะดวกร่วมงาน) |
| อยากบอกอะไรบ่าวสาวไหม | textarea | — | แสดงเสมอ ไม่ว่าจะมาหรือไม่มา |

**ฟิลด์ที่ตัดออกไปแล้ว (เจ้าของขอตัดทิ้งทั้งหมด อย่าเพิ่มกลับมาโดยไม่ถาม):** "สะดวกรับการ์ดไว้เป็นที่ระลึกไหม" (เคยมีไว้ให้กลุ่มไม่สะดวกร่วมงาน), checkbox "ต้องการให้ส่งการ์ดทางไปรษณีย์" (เคยมีทั้งสองกลุ่ม), "ที่อยู่จัดส่ง", "เบอร์โทร" — **ฟอร์มนี้ไม่เก็บข้อมูลการจัดส่งการ์ด/ที่อยู่/เบอร์โทรอีกต่อไปเลย** ไม่ว่าจะกลุ่มไหน (เคยมีกลไก `updateMailVisibility()` คุมการโชว์ผสมสองกลุ่ม — ลบทิ้งไปพร้อมฟิลด์แล้ว ถ้าเห็นชื่อฟังก์ชันนี้โผล่มาอีกในโค้ดแปลว่ามีคนลืมลบ)

คอลัมน์ในชีตเรียงตามนี้ โดยมี `เวลาบันทึก` เป็นคอลัมน์แรก รวม 13 คอลัมน์ทั้งหมด — **ไม่มีคอลัมน์ "ชื่อที่ต้องการให้ปรากฏบนการ์ด", "ท่านสะดวกรับการ์ดแบบใด", "ที่อยู่จัดส่ง", "เบอร์โทร" แยกแล้ว** (ตัดออกไปพร้อมฟิลด์) แทนที่ด้วย **"ชื่อผู้ติดตามคนที่ 1" ถึง "5"** (5 คอลัมน์รวด คนที่ 5 เป็นต้นไปยัดรวมกันในคอลัมน์ที่ 5) — แถวของคนที่ "ไม่สะดวกร่วมงาน" จะมีคอลัมน์ มีผู้ติดตามมาด้วยกันกี่ท่าน/ชื่อผู้ติดตามทั้ง 5/อาหารที่ทานไม่ได้ ว่างเสมอ (ตั้งใจ ไม่ใช่บั๊ก — กลุ่มนี้ไม่ถูกถามฟิลด์พวกนี้เลย)

### หน้า "Thank you" มี 2 ข้อความ
- สะดวกร่วมงาน: "บันทึกคำตอบของท่านเรียบร้อยแล้ว แล้วพบกันวันที่ 27 ธันวาคมนี้"
- ไม่สะดวกร่วมงาน: "ไม่เป็นไรเลยค่ะ/ครับ ขอบคุณที่สละเวลาตอบกลับมานะคะ/ครับ 🤍 ไว้เจอกันใหม่โอกาสหน้านะคะ/ครับ"

ข้อความทั้งสองอยู่เป็นตัวแปร `DONE_MSG_ATTENDING`/`DONE_MSG_NOT_ATTENDING` ต้นๆ `<script>` ใน `Index.html` แก้ตรงนั้นได้เลยถ้าจะเปลี่ยนคำ

---

## กฎเหล็กฝั่ง Apps Script

### ต้องทำ
1. **`appsscript.json` ต้องมี `"timeZone": "Asia/Bangkok"`** — ค่า default ของ clasp คือ `America/New_York` ทำให้เวลาในชีตเพี้ยน 12 ชั่วโมง ตรวจทุกครั้งก่อน push
2. **`webapp.access` ต้องเป็น `ANYONE_ANONYMOUS`** ไม่งั้นแขกถูกบังคับล็อกอิน Google
3. **`Index.html` ต้องมี `<base target="_top">`** ไม่งั้นลิงก์เปิดค้างใน iframe
4. **`submitRsvp` ต้องหุ้ม `LockService`** และ `.slice()` จำกัดความยาวทุกฟิลด์ก่อนเขียนชีต พร้อม honeypot กันบอท

### ห้ามทำ
1. **ห้ามรัน `clasp create-deployment` เอง** เตรียมให้พร้อมแล้วหยุดรอคำยืนยัน
2. **ห้าม commit `.clasp.json`, `.clasprc.json`, Sheet ID จริง** ใส่ `.gitignore` ตั้งแต่แรก
3. **ห้ามเดาข้อมูลที่ยังไม่มี** ใช้ `<<TODO: ...>>` ที่มองปราดเดียวรู้ว่าปลอม

---

## คำสั่ง (clasp 3.x — ชื่อเปลี่ยนจาก 2.x)

```bash
clasp push
clasp push --watch
clasp create-deployment -d "…"   # ← ห้ามรันเองโดยไม่ถาม
clasp open-web-app
clasp tail-logs
```

คำสั่งเก่าที่ใช้ไม่ได้แล้ว อย่าเขียนลงเอกสาร: `clasp open`, `clasp deploy`, `clasp deploy -i <id>`, `clasp logs`

---

## Definition of done

ถ้าข้อไหนยังไม่ได้ตรวจ ให้บอกว่ายังไม่ได้ตรวจ อย่าสรุปว่าเสร็จ

- [ ] `appsscript.json` timeZone `Asia/Bangkok` และ access `ANYONE_ANONYMOUS`
- [ ] เปิดลิงก์ใน **โหมดไม่ระบุตัวตน** แล้วกรอกจบได้ ไม่ถูกขอล็อกอิน
- [ ] ข้อมูลลงชีตครบทุกคอลัมน์ ตามลำดับในตาราง และเวลาตรงกับเวลาไทย
- [ ] แก้เลขในช่อง "มีผู้ติดตามมาด้วยกันกี่ท่าน" แล้วช่องชื่อผู้ติดตามโผล่/หายตามจำนวน (เลข-1 ช่อง) ทันที
- [ ] เลือก "ไม่สะดวก" แล้วช่องจำนวนคน/ชื่อผู้ติดตาม/อาหารที่แพ้หายไปหมด
- [ ] เว้นชื่อว่างแล้วกดส่ง → ขึ้น error ไม่มีแถวขยะลงชีต
- [ ] ส่งซ้ำสองครั้งติดไม่พัง ไม่เขียนทับแถวเดิม
- [ ] จอ ≥900px แบ่งซ้ายขวาทั้งสองหน้า รูปค้างอยู่ตอนเลื่อน
- [ ] ที่ 390px ไม่มีอะไรล้นขอบ ตัวอักษรไม่โดนลายมุมทับ ปุ่มกดด้วยนิ้วโป้งได้
- [ ] น้ำหนักหน้าเว็บรวม ≤1.2MB ไม่นับเพลง — วัดจริงจาก Network tab
- [ ] ปุ่มเพลงเริ่มที่ปิดเสียง กดแล้วเล่น กดอีกทีหยุด ไอคอนตรงสถานะ
- [ ] เปิดจาก **in-app browser ของ LINE บนมือถือจริง** แล้วทำงานครบ
- [ ] ไม่มี error สีแดงใน console

---

## ยังไม่มีข้อมูล ต้องถามก่อนใช้

- ชื่อ-นามสกุลเต็มของ Poom และ Kanan
- เวลางาน/กำหนดการเต็มรูปแบบ — **ยังไม่ต้องใส่ในหน้านี้** (สถานที่ใส่แล้ว ดูหัวข้อด้านบน)

ได้แล้ว: Sheet ID (ตั้งเป็น Script Property `SHEET_ID` ไม่ได้เขียนลงไฟล์), รูปทั้ง 3 รูป (cover/rsvp/thankyou อยู่ใน `assets-src/` และขึ้น jsDelivr แล้ว), GitHub repo (`ppuvitt/poom-kanan-rsvp`), สถานที่จัดงาน (Ava Trivi Studio + ลิงก์ Google Maps), เพลงพื้นหลัง (`assets-src/bgm.mp3`)

**ตัดสินใจแล้วว่าไม่เอา (อย่าถามซ้ำ):**
- **กำหนดวันปิดรับคำตอบ** — เจ้าของบอกไม่เอา ไม่ต้องมี deadline บนหน้านี้
- **ลายกุหลาบมุม** — เจ้าของบอกไม่เอาแล้ว หลังจากพังมาสองรอบ (ดูหัวข้อ "ลายกุหลาบมุม" ด้านบนเพื่อรู้ประวัติ แต่ไม่ต้องทำใหม่อีก)

---

## วิธีทำงานกับเจ้าของโปรเจกต์

- เขาชอบให้แย้งตรง ๆ ถ้าเขาขออะไรที่จะทำให้ช้าลงหรือพังในมือถือ ให้บอกก่อนทำ
- อย่าตอบว่า "เรียบร้อยแล้ว" ถ้ายังไม่ได้ตรวจตาม Definition of done
- **งานนี้ตัดสินด้วยตา** ถ้ามีเครื่องมือแคปหน้าจอ (Chrome DevTools MCP หรือ Playwright MCP) ให้เปิดหน้าเว็บดูเองทุกครั้งก่อนบอกว่าเสร็จ ถ้าไม่มี ให้บอกตรง ๆ ว่ายังไม่ได้ตรวจด้วยตา อย่าเดา
- เวลาให้เขาไปกดอะไรใน Google Console ให้บอกเป็นขั้น ๆ พร้อมชื่อปุ่มที่เห็นบนหน้าจอจริง
